# TruthLens 全面测试报告（2026-09-05）

> 测试对象：`E:\360MoveData\Users\Administrator\Desktop\真实\Truthlens`（AI 幻觉检测系统：FastAPI 后端 + React/Vite 前端 + Chrome MV3 扩展）
> 测试方式：首轮测试只记录、未改源码（§1–§10）；随后用户指示修复，已对缺陷 A/B 修复并复测通过（见 §11），改动 3 个文件：`backend/app/main.py`、`backend/app/api/conversations.py`、新增迁移 `backend/alembic/versions/003_documents_conv_nullable.py`。其余观察项仍只记录。
> 附带 1 个可复用 API 冒烟脚本 + 2 个专项脚本（见 §9），发现问题清单见 §10。

---

## 0. 环境与前置判断

- 工作区 git 有**大量未提交改动**：30+ 文件修改，新增 `apikeys / settings / openai-compat / analytics` 等 API 模块与前端页面（apidocs / apikeys / settings 页）。
- 测试前发现运行中实例已**过期**：
  - 6655 后端进程 11:17 启动，而 `backend/app` 最新代码改到 **11:34**（settings.py、claim_adjudicator.py、nli_model.py、detect/chat/openai_compat、config.py 等均晚于启动时间）→ 运行中的不是最新代码。
  - `frontend/dist` 产物早于 `src`（api.ts / settings-modal.tsx 修改于 11:28–11:29）。
- 因此本次在**最新代码**上另起独立实例测试：6666（兼容开放模式，与线上同配置）+ 6667（`API_KEY_REQUIRED=true` 强制鉴权实例），前端用最新源码重建后验证，最后把 6655 重启为最新代码（§8）。
- 环境事实：`backend/venv` 为 Python 3.11.9（README 声称 3.13，但不影响运行）；Postgres 容器 `ai_hallucination_detection_system-postgres-1`（host 5433→5432，UTC 时钟，正常）。`.env` 中已配置 GROQ / ZEN(含 base_url+model) / TAVILY / SERPER；NVIDIA / OPENROUTER / GEMINI / Google FactCheck 为空。
- 进程形态：venv 的 `python.exe`（重定向器）+ 系统 Python311 工作进程成对出现属正常现象（每个实例 = 2 个 PID，非重复启动缺陷）。

---

## 1. 后端静态检查 ✅

| 项目 | 结果 |
|---|---|
| `python -m compileall app`（全部后端代码） | ✅ 通过 |
| `compileall` 根目录 9 个 test/check 脚本 | ✅ 通过 |

---

## 2. 后端自带测试脚本

### 2.1 纯单元（无外部调用）— 全部 PASS
- `test_risk_scorer.py` ✅：混合（2 已证+1 不可达源）总分 10.0（要求≤20）；全不可达=55.0；空=0；警告含 `unverifiable_source`。
- `test_threshold_sweep.py` ✅：13/13 命中，判定 **KEEP-CURRENT**（当前阈值 0.8/0.7/0.5 已最优）。
- `test_settings_sync.py` ✅：下拉名 / 网关映射 / 全新单例 / 7 个单例热重载重置 / 生效摘要，全部断言通过。

### 2.2 真实验证类（真实调用第三方）— 全部 PASS
- `test_nli_groq.py` ✅：Groq 判定器 `openai/gpt-oss-20b`，6/6 正确（EN/ZH 各 3 组 entailment/contradiction/neutral），约 4.0s。
- `test_router_timing.py` ✅：1 条声明取证 8 条证据，5.8s（tavily 5 + direct_api 3）。
- `test_adjudicate_joint.py` ✅（含决策差异观察）：per-claim vs joint 一致性 **2/4**，按规则维持 per-claim；差异样例 c3（coffee/longevity：perclaim CONTRADICTED/82 vs joint UNVERIFIED/55）、c4（Python 诞生时间：CONTRADICTED/80 vs PARTIALLY_VERIFIED/30）→ 两种裁决口径对"时间偏移/部分支持"类声明的分歧值得关注。
- `check_all_models.py` ✅：GROQ 目录含 `openai/gpt-oss-120b / gpt-oss-20b / groq/compound` 等；NVIDIA/GEMINI=No key；OpenRouter 免费模型可达。

### 2.3 有问题的脚本
- `test_all_models.py` ❌ **不可用**：无 GEMINI key 时 `genai.Client()` 直接抛 `ValueError: No API key` 中断（脚本未对缺 Key 环境做跳过）。
- `test_api_server.py` ❌ **过期脚本**：硬编码 `http://localhost:8000`（实际服务在 6655/6666），连接被拒；也未加载 `.env`。需要同步端口/配置，否则会误导。

---

## 3. 前端检查与构建

| 项目 | 结果 |
|---|---|
| `tsc -b`（类型检查，随 build） | ✅ 通过 |
| `vite build` | ✅ 通过（27s；产物 `dist/assets/index-CBTY4JoR.js`，约 1.0 MB） |
| 构建告警 | ⚠️ chunk > 500 kB，建议代码分割 |
| `npm run lint` (eslint) | ❌ **28 errors + 1 warning** |

**Lint 失败主要集中在新增代码**（并非纯风格）：
- `src/lib/api.ts`：多行 `no-explicit-any`；**20 行的 `no-unused-expressions` 与 `no-empty`（疑似残缺逻辑表达式/空 catch）**；281/476/479/493/496/501 行 any。
- `src/components/apikeys/apikeys-page.tsx`：22/23/36 行空块与 any；`apidocs-page.tsx` 37/38 行 any；`settings-modal.tsx` 177 行空块。
- `src/components/sidebar/sidebar-container.tsx`：useEffect 缺依赖（warning）。
- 依赖的三方/样例组件（animate-ui、ui/*.tsx、badge/button/grid-pattern）：`react-hooks/purity`（Math.random 于渲染期）、`react-refresh/only-export-components`、`no-explicit-any`。
→ 结论：类型/构建绿，但 **lint 门禁未通过，新功能页含 should-be-fixed 的表达式/空块问题**。

---

## 4. 浏览器扩展检查 ✅

- `manifest.json`：v0.3.0 / MV3，JSON 解析正常；content_scripts 指向的 4 个文件（chatgpt/gemini/claude/deepseek attachment-observer）与 background.js 均存在。
- 14 个 `.js` 全部通过 `node --check` 语法检查（含 background2.js、各 extractor、dom、highlight-normalizer 等）。
- 注意：manifest 只挂 4 个站点观察器（copilot 有 extractor 但未注册为 content script），属配置取舍，未判为错误。

---

## 5. 全量 API 冒烟（实例 6666，最新代码，兼容开放模式）

第一轮 `api_full_smoke_test.py`：**53 用例 → 46 PASS / 7 FAIL**，其中 3 个是我的脚本自身缺陷、2 个是产品缺陷、2 个是断言预期写错（随后在第二轮修正并复测）。

通过项（节选）：/health、/docs、/openapi.json；/v1/models（返回 3 个已配 Key 模型：gpt-oss-120b / gpt-oss-20b / mimo-v2.5）；/api/v1/models；apikeys 全生命周期（status/list/create/明文一次性/带 Key 调用计数递增/无效 Key 兼容放行）；settings schema/status/effective/test 9 项（已保存 GROQ、TAVILY 连通 OK；错误草稿 Key、GEMINI 未配置、未知 Key 均正确报失败）；conversations 创建/列表/加 2 条消息/详情；documents 列表/带会话上传/元数据/删除/删除后 404/空文件 400；analytics days=7（含 timeline/models/api_keys/providers 等字段）与 days=0/91→422；chat 非流式（真模型回复 "OK"）与 SSE 流式；未知 model_id→400、无 Key 模型 gemini-3-flash-preview→400；openai 兼容非流式/流式（chat.completion.chunk + [DONE]）/ `model="groq"` 简写映射/空 messages→400；detect 两模式（见 §6）；PUT /settings（空 values 幂等）。

**第二轮 `api_full_smoke_test_part2.py`：12 用例 → 9 PASS / 3 FAIL**（后两个 FAIL 均为已确认产品缺陷，见 §7；第 1 个是修正后的预期，见 §6 观察项）。

### 结构/语义观察（第二轮）
- **detect 非法 role 实际返回 200 并跑完整流程**（不是 422）：`ConversationMessage.role` 未限制为 user/assistant，属宽松设计（不是校验缺陷，但 API 文档声称 role 仅两类）。
- 未知**非 API** 路由返回 index.html（SPA 回退，设计如此）；未知 `/api/*` 路由正确 404。

---

## 6. detect 主流程（真实端到端，含外部调用）

| 用例 | 结果 |
|---|---|
| 中文文本 + 联网验证（"埃菲尔铁塔高330米…1889年完工"） | ✅ risk=5.0 LOW，4 条声明**全 VERIFIED**，约 9s；首条证据 4 条引用 |
| 文档知识库通道 | 未跑通——受 §7 缺陷 A 阻塞（全局 KB 上传即 500） |
| 扩展(extension)模式（platform+messages[]） | ✅ risk=5.0 LOW，1 条声明 VERIFIED（引用 5 条），约 34s |
| 空 body（两种模式都不满足） | ✅ 400 且中文提示两种合法请求格式 |

**观察 1**：两条 VERIFIED 声明的 `reasoning` 都是 `"Fallback heuristic: max_entailment=…"`；服务端 detect 全程只记录了 **groq** provider 用量（无 zen/gemini）→ 高置信蕴含时裁决走了启发式捷径，**未消耗 LLM**，属实现选择，但需确认这是有意为之（省成本）还是应调 LLM 出理由。
**观察 2**：整体风险分偏低（4/4 全 VERIFIED 仍 5.0/LOW），风险标定偏保守，建议结合真实误报样本复核阈值。

---

## 7. 产品缺陷（原 2 个 —— 已修复并复测通过，修复记录见 §11；本节约保留复现与根因）

### 缺陷 A：全局知识库文档上传必 500（`POST /api/v1/documents/upload` 不带 conversation_id）
- 现象：上传不带 `conversation_id`（= 全局知识库通道，前端"Upload Doc"及扩展捕获走此路径）→ **HTTP 500** `IntegrityError: null value in column "conversation_id" of relation "documents" violates not-null constraint`。
- 根因：`app/db/models.py` 中 `Document.conversation_id` 已是 `nullable=True`，但**所有 alembic 迁移（001 起）均将 `documents.conversation_id` 建成 NOT NULL，且没有任何迁移把它改为可空**；`app/main.py` 启动自愈 DDL 只处理了 api_keys/provider_usage/analysis_results.model_id，**漏了 documents** → 现有/新装数据库上该路径必然失败。
- 佐证：同一接口**带** conversation_id 上传 → 200 正常（§5）。
- 修复（已执行，见 §11）：新增 alembic 迁移 `003_documents_conv_nullable` + `main.py` 启动自愈 DDL `DROP NOT NULL`。

### 缺陷 B：`POST /api/v1/conversations/sync` 的 `total_messages` 恒少算本次新增
- 现象（稳定复现）：首次 sync 2 条 → 返回 `messages_synced=2, total_messages=0`；再次 sync 1 条 → `messages_synced=1, total_messages=2`（实际库内已是 3 条，随后 `GET /conversations/{id}` 证实 =3）。
- 根因：`app/api/conversations.py` sync 处理器在**统计总数之后才 commit**，而新插入行在计数查询时尚未对当前事务可见（pending 未 flush/commit）→ 返回的 `total_messages` 永远漏掉本次新增；`messages_synced` 是用内存计数所以正确。
- 影响：依赖 `total_messages` 的客户端（扩展状态显示等）计数不准。
- 修复（已执行，见 §11）：sync 处理器在统计总数前显式 `await db.flush()`（会话 autoflush=False，必须手动 flush 让新增行对本事务可见）。

---

## 8. 强制鉴权模式验证（实例 6667：`API_KEY_REQUIRED=true` + 主 Key）— 18/18 PASS

- 公开端点放行：/health、/apikeys/status（管理页设计无鉴权）。
- 强制拦截 ✅：`/v1/models`、`/v1/chat/completions`、`/api/v1/chat`、`/api/v1/detect` 无 Key → **401**（中文报错）。
- 无效 Key（Bearer / X-API-Key）→ 401 ✅；主 Key（Bearer / X-API-Key）→ 200 ✅。
- 子 Key 生命周期：创建 → 调用放行 200 → 吊销 → 再调用 **401** ✅。
- **安全观察（重要）**：强制模式下 `conversations / documents / analytics / settings / apikeys` 等数据/管理端点**完全未挂鉴权依赖，仍无 Key 可访问**（GET /conversations、GET /settings/status、GET /analytics/overview、GET /documents 均 200）。当前靠"内网/防火墙隔离"的设计假设，但若按注释意图"公网部署切 API_KEY_REQUIRED=true 强制"，这些端点会裸奔——`PUT /settings` 还能改写 .env 与热重载 Key。建议至少给数据端点上可选鉴权。

---

## 9. 复测/收尾与当前运行状态

- 前端：最新源码 `tsc+vite build` 成功；产物已按启动脚本同款方式发布到 `frontend_dist/` 与 `backend/frontend_dist/`（并清掉旧 hash 残留）；验证 `GET /` 已引用新产物 `index-CBTY4JoR.js`、资源 200。
- 6655 已在修复后再次重启并处于**最终运行状态**：最新代码（含缺陷 A/B 修复）+ 新构建 WebUI，/health、/api/v1/apikeys/status 正常，WebUI 为新构建（`index-CBTY4JoR.js`）。仍为兼容开放模式（与最初一致，`.env` 未配置主 Key/强制）。
- 测试实例 6666 / 6667 已停止；测试产生的数据已从数据库清理（4 条 analysis + 级联 claims/evidence 54+9、1 条测试会话；测试 apikey 均已吊销/清除）；`.env` 与测试前逐字节一致（有备份比对），备份已删除。
- 测试产生/可复用文件（Truthlens 根目录）：
  - `api_full_smoke_test.py`（53 用例主套件）
  - `api_full_smoke_test_part2.py`（12 用例复测/缺陷复现）
  - `api_auth_enforcement_test.py`（18 用例强制鉴权）
  - `api_suite_out.log` / `api_suite_part2_out.log` / `api_auth_out.log`（原始输出）

---

## 10. 问题清单（按优先级）

| # | 级别 | 问题 | 位置 |
|---|---|---|---|
| 1 | 🔴 高 | 全局知识库上传必 500：documents.conversation_id NOT NULL 未随模型改可空（无迁移/无启动自愈 DDL） | 缺陷 A —— **已修复（§11）** |
| 2 | 🟠 中 | sync 响应 `total_messages` 漏算本次新增（计数先于 flush/commit） | 缺陷 B —— **已修复（§11）** |
| 3 | 🟠 中 | 强制鉴权模式下 conversations/documents/analytics/settings/apikeys 管理端点无鉴权，公网裸奔 | api 各 router 依赖 |
| 4 | 🟡 中 | eslint 28 错误：新页 apikeys/apidocs/settings 与 `lib/api.ts`（含 api.ts:20 空表达式/空块疑似逻辑残片） | frontend/src |
| 5 | 🟡 低 | 测试脚本过期/不可用：`test_api_server.py` 写死 :8000；`test_all_models.py` 无 GEMINI key 即崩溃 | backend/ |
| 6 | 🔵 观察 | detect 高置信声明走"Fallback heuristic"，服务端 detect 全程无 zen/gemini 裁决调用；裁决启发式文案对用户可见 | claim_adjudicator |
| 7 | 🔵 观察 | 裁决 joint vs per-claim 分歧 2/4（时间偏移/部分支持类声明）；风险分整体偏低（4/4 已证仍 LOW/5） | 裁决/标定 |
| 8 | 🔵 观察 | 前端 chunk 1.0MB >500KB；role 字段未限枚举；中文控制台输出需 `-X utf8`（显示乱码，不影响功能） | 构建/模型 |

---

## 11. 修复记录（缺陷 A / B —— 本次会话修复并复测通过）

> 背景：测试阶段按"只记录"处理；随后用户指示修复，以下改动即修复内容。

### 改动文件（3 处）
1. **`backend/app/main.py`**（启动自愈 DDL）：在已有 `analysis_results.model_id` 自愈 ALTER 之后新增
   `ALTER TABLE documents ALTER COLUMN conversation_id DROP NOT NULL` —— 老库/任意库首次启动即自愈，幂等。
2. **`backend/app/api/conversations.py`**（sync 处理器）：在 `sync_messages_to_conversation(...)` 之后、统计总数之前插入
   `await db.flush()` —— 数据库会话 `autoflush=False`（见 `app/db/engine.py`），不 flush 则新增行对同一事务内的 count 查询不可见，导致 `total_messages` 恒漏本次新增。
3. **`backend/alembic/versions/003_documents_conv_nullable.py`**（新迁移）：`documents.conversation_id` 由 NOT NULL 改为可空；因原迁移树有两条历史分支（head：`002_exact_quote` 与 `afcad0290ea1`），该迁移同时以二者为 `down_revision`，执行后合并为单一 head，保证**全新部署 `alembic upgrade head` 也正确**。

### 已执行动作
- `alembic upgrade head` 成功：`alembic_version` 由 2 行（002_exact_quote、afcad0290ea1）变为单行 `003_documents_conv_nullable`；
  实测 `documents.conversation_id is_nullable = YES`。
- 6655 以修复后代码重启，启动日志无 Traceback/ERROR。

### 复测结果（直连运行中 6655 实测）
- **缺陷 A**：不带 `conversation_id` 上传 → **HTTP 200**（chunk_count=1，元数据/删除均 200）✅（修复前恒 500）。
- **缺陷 B**：sync 导入 2 条 → `messages_synced=2, total_messages=2`；再导入 1 条 → `messages_synced=1, total_messages=3`；随后 `GET /conversations/{id}` 实际条数 =3，与 total 一致 ✅（修复前首次 total=0）。
- 复测产生的测试数据已清理；`.env` 未被改动。
- 复测脚本保留：`Truthlens/verify_fixes.py`（可随时重跑，需 6655 在线）。
